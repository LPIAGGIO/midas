{
  "name": "jarvis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jarvis: asistente autonomo personal. Cerebro en el VPS (Claude Agent SDK), memoria en Supabase, clientes intercambiables (Telegram v1, web y ESP32 despues).",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "check": "node --check worker.js && node --check lib/agent.js && node --check lib/policy.js && node --check lib/db.js && node --check lib/telegram.js",
    "selftest": "node selftest.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.227",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5"
  }
}
